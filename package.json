{
  "name": "fastify-now",
  "version": "1.0.0",
  "description": "This directory is a brief example of a vanilla site that can be deployed with ZEIT Now and zero configuration.",
  "main": "index.js",
  "scripts": {
    "start": "npm run sass",
    "sass": "node-sass --watch scss -o public --output-style compressed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^2.10.0",
    "fastify-static": "^2.5.0",
    "handlebars": "^4.7.7",
    "point-of-view": "^3.7.0"
  },
  "engines": {
    "node": "10.x"
  }
}
